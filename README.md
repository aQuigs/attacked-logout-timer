# Attacked Logout Timer
RuneLite plugin for displaying a timer when you are attacked to count down until you are able to log out

## Developer setup

1. Install python
1. Install pip
1. `pip install pre-commit`
1. `pre-commit install`
